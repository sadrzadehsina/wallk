{
  "$schema": "https://turbo.build/schema.json",
	"globalEnv": ["GITHUB_ID", "GITHUB_SECRET"],
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
